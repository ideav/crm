# IntegramDataView Component

## 🎯 Обзор

**IntegramDataView** — это полностью изолированный, модульный JavaScript компонент для отображения и управления данными из Integram API в табличном формате. Это самостоятельный продукт, который может быть интегрирован в любое веб-приложение независимо от других компонентов.

## 📦 Состав компонента

Компонент разделен на **три независимых модуля**:

### 1. **IntegramTables** - Табличное представление данных
Обрабатывает данные из эндпоинтов `object/{table_type_id}`:
- Загрузка данных из таблиц с метаданными
- Полная поддержка CRUD операций
- Автоматическая генерация форм на основе метаданных
- Поддержка родительских связей через `F_U` параметр

### 2. **IntegramReports** - Представление отчетов
Обрабатывает данные из эндпоинтов `report/{report_id}`:
- Загрузка данных из отчетов
- Read-only представление с продвинутой фильтрацией
- Оптимизировано для больших объемов данных
- Поддержка пользовательских параметров отчетов

### 3. **IntegramControls** - Общие функции и утилиты
Предоставляет общую функциональность для обоих модулей:
- Форматирование дат и чисел
- Экранирование HTML для защиты от XSS
- Управление cookies
- Утилиты для работы с фильтрами
- Рендеринг модальных окон
- Обработка ошибок и загрузок

## ✨ Основные возможности

### Управление данными
- ✅ Множественные источники данных (таблицы и отчеты)
- ✅ Бесконечная прокрутка (infinite scroll)
- ✅ Продвинутая фильтрация (13+ операторов)
- ✅ Inline редактирование с поддержкой различных типов данных
- ✅ Управление ссылочными полями с автодополнением
- ✅ Drag & drop для изменения порядка колонок
- ✅ Управление видимостью колонок
- ✅ Сохранение состояния через cookies
- ✅ Пользовательское стилизование ячеек

### Пользовательский интерфейс
- ✅ Адаптивный дизайн
- ✅ Material Design стилизация
- ✅ Sticky заголовки таблицы
- ✅ Компактный/просторный режимы отображения
- ✅ Динамическое создание колонок
- ✅ Настраиваемые фильтры для каждой колонки
- ✅ Модальные окна для форм

### Типы данных
- 📝 Текст (CHARS, SHORT, MEMO)
- 🔢 Числа (NUMBER, SIGNED)
- 📅 Даты (DATE, DATETIME)
- 🔗 Ссылки (foreign keys с автодополнением)
- ☑️ Логические значения (checkboxes)

## 🚀 Быстрый старт

### Установка

1. Подключите файлы компонента:
```html
<link rel="stylesheet" href="assets/css/integram-data-view.css">
<script src="assets/js/integram-data-view.js"></script>
```

2. Создайте контейнер в HTML:
```html
<div id="my-table"></div>
```

3. Инициализируйте компонент в JavaScript:

#### Для таблиц (Tables):
```javascript
const tableView = new IntegramTables('my-table', {
    apiUrl: 'https://api.integram.ru',
    tableTypeId: 123,
    parentId: 456,
    title: 'Мой каталог',
    pageSize: 50
});
```

#### Для отчетов (Reports):
```javascript
const reportView = new IntegramReports('my-table', {
    apiUrl: 'https://api.integram.ru/report/789',
    title: 'Отчет по продажам',
    pageSize: 50
});
```

### Автоматическая инициализация через HTML

Компонент поддерживает автоматическую инициализацию через data-атрибуты:

#### Для таблиц:
```html
<div id="products-table"
     data-integram-type="table"
     data-api-url="https://api.integram.ru"
     data-table-type-id="123"
     data-parent-id="456"
     data-title="Каталог продуктов"
     data-page-size="50">
</div>
```

#### Для отчетов:
```html
<div id="sales-report"
     data-integram-type="report"
     data-api-url="https://api.integram.ru/report/789"
     data-title="Отчет по продажам"
     data-page-size="50">
</div>
```

Компонент автоматически инициализируется при загрузке страницы (`DOMContentLoaded`).

## 📖 Детальная документация

### Параметры конфигурации

#### Общие параметры (для обоих модулей)

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `apiUrl` | string | '' | Базовый URL для API запросов |
| `title` | string | '' | Заголовок таблицы |
| `pageSize` | number | 20 | Количество записей на странице |
| `cookiePrefix` | string | 'integram-view' | Префикс для cookies |
| `instanceName` | string | auto | Имя глобальной переменной |
| `onCellClick` | function | null | Callback при клике на ячейку |
| `onDataLoad` | function | null | Callback после загрузки данных |
| `parentId` | string/number | null | ID родительской записи |

#### Специфичные для IntegramTables

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `tableTypeId` | string/number | **обязательно** | ID типа таблицы |

### API методы

#### Общие методы (IntegramTables и IntegramReports)

```javascript
// Загрузить данные
tableView.loadData(append = false);

// Переключить фильтры
tableView.toggleFilters();

// Очистить все фильтры
tableView.clearAllFilters();

// Получить общее количество записей
tableView.fetchTotalCount();

// Открыть настройки таблицы
tableView.openTableSettings();

// Открыть настройки колонок
tableView.openColumnSettings();

// Сохранить состояние колонок
tableView.saveColumnState();

// Сохранить настройки таблицы
tableView.saveSettings();
```

#### Методы IntegramControls (статические утилиты)

```javascript
// Экранирование HTML
IntegramControls.escapeHtml('<script>alert("xss")</script>');

// Форматирование дат
IntegramControls.formatDate('20260207', 'DATE');  // '07.02.2026'
IntegramControls.formatDate('202602071430', 'DATETIME');  // '07.02.2026 14:30'

// Работа с cookies
IntegramControls.setCookie('key', 'value', 365);
IntegramControls.getCookie('key');

// Обрезка текста
IntegramControls.truncateText('very long text...', 127);

// Извлечение базового URL
IntegramControls.extractApiBase('https://api.integram.ru/report/123?param=1');

// Debounce функция
const debouncedFunction = IntegramControls.debounce(() => {
    console.log('Called after 500ms');
}, 500);

// Модальные окна
IntegramControls.renderModal('Заголовок', '<p>Содержимое</p>', () => {
    console.log('Modal closed');
});

// Индикаторы загрузки
IntegramControls.showLoading(container, 'Загрузка данных...');

// Сообщения об ошибках
IntegramControls.showError(container, 'Произошла ошибка');
```

### Операторы фильтрации

Компонент поддерживает следующие операторы фильтрации:

#### Для текстовых полей (CHARS, SHORT, MEMO)
| Символ | Название | Описание |
|--------|----------|----------|
| `^` | начинается с... | Значение начинается с введенного текста |
| `=` | равно | Точное совпадение |
| `≠` | не равно | Не равно введенному значению |
| `~` | содержит | Содержит введенный текст |
| `!` | не содержит | Не содержит введенный текст |
| `!^` | не начинается | Не начинается с введенного текста |
| `%` | не пустое | Поле заполнено |
| `!%` | пустое | Поле пустое |
| `(,)` | в списке | Значение в списке (через запятую) |
| `$` | заканчивается | Заканчивается введенным текстом |

#### Для числовых полей (NUMBER, SIGNED)
| Символ | Название | Описание |
|--------|----------|----------|
| `^` | начинается с... | Число начинается с введенных цифр |
| `=` | равно | Равно введенному числу |
| `≠` | не равно | Не равно введенному числу |
| `≥` | не меньше | Больше или равно |
| `≤` | не больше | Меньше или равно |
| `>` | больше | Строго больше |
| `<` | меньше | Строго меньше |
| `...` | в диапазоне | Между двумя значениями (разделитель `\|`) |
| `%` | не пустое | Поле заполнено |
| `!%` | пустое | Поле пустое |

#### Для дат (DATE, DATETIME)
| Символ | Название | Описание |
|--------|----------|----------|
| `=` | равно | Точная дата |
| `≥` | не меньше | Дата не раньше указанной |
| `≤` | не больше | Дата не позже указанной |
| `>` | больше | Дата позже указанной |
| `<` | меньше | Дата раньше указанной |
| `...` | в диапазоне | Между двумя датами (разделитель `\|`) |
| `%` | не пустое | Дата заполнена |
| `!%` | пустое | Дата пустая |

**Использование:** Кликните на символ фильтра для переключения между операторами.

### События и callbacks

#### onDataLoad
Вызывается после успешной загрузки данных:
```javascript
new IntegramTables('my-table', {
    apiUrl: 'https://api.integram.ru',
    tableTypeId: 123,
    onDataLoad: (data) => {
        console.log('Loaded rows:', data.length);
        console.log('First row:', data[0]);
    }
});
```

#### onCellClick
Вызывается при клике на ячейку:
```javascript
new IntegramReports('my-report', {
    apiUrl: 'https://api.integram.ru/report/789',
    onCellClick: (rowIndex, columnIndex, value, column, row) => {
        console.log('Clicked:', { rowIndex, columnIndex, value, column, row });
    }
});
```

### Кастомизация стилей

Компонент использует CSS переменные для легкой кастомизации:

```css
:root {
    /* Основные цвета */
    --integram-primary: #1976d2;
    --integram-primary-dark: #1565c0;
    --integram-primary-light: #42a5f5;
    --integram-accent: #ff4081;

    /* Цвета текста */
    --integram-text-primary: rgba(0, 0, 0, 0.87);
    --integram-text-secondary: rgba(0, 0, 0, 0.54);

    /* Фоны */
    --integram-background: #fafafa;
    --integram-surface: #ffffff;

    /* Отступы */
    --integram-spacing-xs: 4px;
    --integram-spacing-sm: 8px;
    --integram-spacing-md: 16px;
    --integram-spacing-lg: 24px;
}
```

Переопределите эти переменные в вашем CSS для изменения внешнего вида:

```css
.my-custom-theme {
    --integram-primary: #673ab7;
    --integram-accent: #ffc107;
    --integram-surface: #f5f5f5;
}
```

## 🔧 Интеграция с Integram API

### Формат данных для Tables (object/{table_type_id})

#### Запрос метаданных
```
GET /type/{type_id}/?JSON_DATA
```

Ответ:
```json
{
    "f": [
        {
            "i": "column_id",
            "n": "Column Name",
            "t": "CHARS",
            "f": "SHORT",
            "o": "original_table_id",
            "g": 1
        }
    ]
}
```

#### Запрос данных
```
GET /object/{type_id}/?JSON_DATA&F_U={parent_id}&LIMIT=20&OFFSET=0
```

Ответ:
```json
{
    "d": [
        {
            "column_id": "value1",
            "another_column": "value2"
        }
    ]
}
```

### Формат данных для Reports (report/{report_id})

#### Запрос данных
```
GET /report/{report_id}?LIMIT=20&OFFSET=0
```

Ответ:
```json
{
    "f": [
        {
            "i": "col1",
            "n": "Column 1",
            "t": "CHARS"
        }
    ],
    "d": [
        {
            "col1": "value1",
            "col2": "value2"
        }
    ]
}
```

## 🎨 Примеры использования

### Пример 1: Простая таблица продуктов

```html
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="assets/css/integram-data-view.css">
</head>
<body>
    <div id="products"></div>

    <script src="assets/js/integram-data-view.js"></script>
    <script>
        const products = new IntegramTables('products', {
            apiUrl: 'https://api.integram.ru',
            tableTypeId: 100,
            title: 'Каталог продуктов',
            pageSize: 30,
            onDataLoad: (data) => {
                console.log(`Загружено ${data.length} продуктов`);
            }
        });
    </script>
</body>
</html>
```

### Пример 2: Отчет с фильтрацией

```html
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="assets/css/integram-data-view.css">
</head>
<body>
    <div id="sales-report"></div>

    <script src="assets/js/integram-data-view.js"></script>
    <script>
        const report = new IntegramReports('sales-report', {
            apiUrl: 'https://api.integram.ru/report/500',
            title: 'Отчет по продажам за месяц',
            pageSize: 50
        });

        // Включить фильтры по умолчанию
        report.filtersEnabled = true;
        report.render();
    </script>
</body>
</html>
```

### Пример 3: Использование IntegramControls

```javascript
// Форматирование даты
const formattedDate = IntegramControls.formatDate('20260207', 'DATE');
console.log(formattedDate); // '07.02.2026'

// Экранирование HTML
const safe = IntegramControls.escapeHtml('<script>alert("xss")</script>');
console.log(safe); // '&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;'

// Обрезка текста
const truncated = IntegramControls.truncateText('Very long text that needs truncation', 20);
console.log(truncated);
// { text: 'Very long text that ', isTruncated: true, fullText: '...' }

// Debounce
const searchHandler = IntegramControls.debounce((query) => {
    console.log('Searching for:', query);
}, 500);

document.querySelector('#search').addEventListener('input', (e) => {
    searchHandler(e.target.value);
});
```

### Пример 4: Кастомная тема

```html
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="assets/css/integram-data-view.css">
    <style>
        .dark-theme {
            --integram-primary: #bb86fc;
            --integram-background: #121212;
            --integram-surface: #1e1e1e;
            --integram-text-primary: rgba(255, 255, 255, 0.87);
            --integram-text-secondary: rgba(255, 255, 255, 0.60);
            --integram-divider: rgba(255, 255, 255, 0.12);
        }
    </style>
</head>
<body class="dark-theme">
    <div id="my-table"
         data-integram-type="table"
         data-api-url="https://api.integram.ru"
         data-table-type-id="123"
         data-title="Темная тема">
    </div>

    <script src="assets/js/integram-data-view.js"></script>
</body>
</html>
```

## 📱 Адаптивность

Компонент полностью адаптивен и корректно работает на всех устройствах:

- **Desktop** (> 768px): полный функционал с широкими колонками
- **Tablet** (768px - 480px): сжатый режим с уменьшенными отступами
- **Mobile** (< 480px): компактный режим с минимальными отступами

Автоматически применяются медиа-запросы для оптимального отображения.

## 🔒 Безопасность

### Защита от XSS
Все пользовательские данные автоматически экранируются через `IntegramControls.escapeHtml()`:
- Значения ячеек
- Заголовки колонок
- Параметры фильтров
- Текст в модальных окнах

### Защита от CSRF
Рекомендуется использовать CSRF токены для всех модифицирующих запросов:
```javascript
// Добавьте CSRF токен в заголовки запросов
const originalFetch = window.fetch;
window.fetch = function(url, options = {}) {
    options.headers = options.headers || {};
    options.headers['X-CSRF-Token'] = document.querySelector('meta[name="csrf-token"]').content;
    return originalFetch(url, options);
};
```

### Управление cookies
Компонент использует cookies только для сохранения состояния UI:
- Порядок колонок
- Видимость колонок
- Настройки таблицы (компактный режим, размер страницы)
- Ширина колонок

Никакие чувствительные данные не сохраняются в cookies.

## 🐛 Отладка

### Режим отладки
Включите детальное логирование в консоль:
```javascript
const tableView = new IntegramTables('my-table', {
    apiUrl: 'https://api.integram.ru',
    tableTypeId: 123
});

// Включить логирование
tableView.debug = true;
```

### Проверка состояния
```javascript
// Проверить загруженные данные
console.log('Data:', tableView.data);

// Проверить колонки
console.log('Columns:', tableView.columns);

// Проверить фильтры
console.log('Filters:', tableView.filters);

// Проверить настройки
console.log('Settings:', tableView.settings);
```

## 🔄 Обновление и миграция

### От integram-table.js к integram-data-view.js

Старый код:
```javascript
const table = new IntegramTable('container', {
    apiUrl: 'https://api.integram.ru/report/123',
    dataSource: 'report'
});
```

Новый код (с разделением на модули):
```javascript
// Для отчетов используйте IntegramReports
const report = new IntegramReports('container', {
    apiUrl: 'https://api.integram.ru/report/123'
});

// Для таблиц используйте IntegramTables
const table = new IntegramTables('container', {
    apiUrl: 'https://api.integram.ru',
    tableTypeId: 456
});
```

## 📊 Производительность

### Оптимизация для больших данных
- Используйте infinite scroll вместо загрузки всех данных сразу
- Установите оптимальный `pageSize` (рекомендуется 20-50)
- Используйте фильтры для ограничения набора данных
- Включайте `truncateLongValues` для обрезки длинных значений

### Рекомендации
```javascript
const optimizedTable = new IntegramTables('my-table', {
    apiUrl: 'https://api.integram.ru',
    tableTypeId: 123,
    pageSize: 30,  // Оптимальный размер страницы
    settings: {
        truncateLongValues: true,  // Обрезать длинные значения
        compact: false  // Просторный режим по умолчанию
    }
});
```

## 🧪 Тестирование

### Ручное тестирование
1. Откройте страницу с компонентом в браузере
2. Проверьте загрузку данных
3. Проверьте работу фильтров
4. Проверьте infinite scroll
5. Проверьте сохранение состояния в cookies

### Автоматизированное тестирование
```javascript
// Пример теста с Jest
describe('IntegramTables', () => {
    test('should load data on init', async () => {
        const container = document.createElement('div');
        container.id = 'test-container';
        document.body.appendChild(container);

        const table = new IntegramTables('test-container', {
            apiUrl: 'https://api.integram.ru',
            tableTypeId: 123
        });

        await table.loadData();

        expect(table.data.length).toBeGreaterThan(0);
        expect(table.columns.length).toBeGreaterThan(0);
    });
});
```

## 📄 Лицензия

Этот компонент разработан для платформы Integram и может использоваться независимо в любом веб-приложении с указанием авторства.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию выше
2. Изучите примеры использования
3. Проверьте консоль браузера на наличие ошибок
4. Откройте issue на GitHub

## 📝 История изменений

### Версия 1.0.0 (2026-02-07)
- ✨ Первый релиз компонента
- ✅ Разделение на три модуля: IntegramTables, IntegramReports, IntegramControls
- ✅ Полная изоляция от integram-table.js
- ✅ Поддержка object/{table_type_id} и report/{report_id} эндпоинтов
- ✅ 13+ операторов фильтрации
- ✅ Infinite scroll пагинация
- ✅ Inline редактирование
- ✅ Drag & drop колонок
- ✅ Адаптивный дизайн
- ✅ Material Design стилизация
- ✅ Автоматическая инициализация через HTML

---

**Разработано для Integram CRM**
**Версия:** 1.0.0
**Дата:** 2026-02-07
**Автор:** AI Issue Solver
