# Отчёты (Запросы) в Integram: методичка, аудит и пилот atex

Дата: 2026-06-01. Общий документ по отчётам Integram; примеры — на боевой БД
ateh (`https://ideav.ru/ateh`).

В Integram **«запрос» (отчёт)** — это объект системной таблицы **22**, который ядро
разворачивает в SQL `SELECT` с автоматическими `LEFT JOIN` по метаданным.
Запуск: `GET /{db}/report/{queryId|имя}?JSON_KV` → плоские сджойненные строки.

## 1. Текущий статус: отчёты не используются

Все рабочие места atex читают данные напрямую через `object/{table}/?JSON_OBJ` и
**джойнят/агрегируют на клиенте**. Слой `report/` упомянут комментарием
«перевод чтений на защищённый слой `report/` — следующий этап» в 10 файлах
(cut-calc, orders, warehouse, slitter, portal, cut-map, intake,
production-planning, sleeve-cutter, dashboards), но нигде не задействован.

## 2. Где отчёты выгодны (из аудита, по убыванию выгоды)

| РМ | Что делает сейчас | Выгода от отчёта |
|----|-------------------|------------------|
| **dashboards.js** | `loadAll` (стр. 461–468) тянет **6 таблиц целиком** + 4 `_count`; `makeRow` (225–274) джойнит Заказ→Позиция→Обеспечение→Резка→ГП в JS | 1 отчёт вместо 6 полных выгрузок и клиентских джойнов |
| **production-planning.js** | `loadPositions`(2000)+`loadCuts`(2000)+`loadSupplies`(5000), джойн на клиенте | отчёт «очередь резок с позициями и обеспечением» |
| **portal.js / orders.js** | Заказ→Позиции | отчёт «заказы с позициями и статусом исполнения» |
| cut-map.js | Резка→Тип резки→Полосы (зависимые загрузки) | кандидат послабее |

Общая выгода: меньше round-trip’ов; серверный join/агрегация (не тянуть тысячи
строк на клиент); защищённый слой (отчёт уважает права роли); нет клиентских
join-багов.

### Правило: минимизируй серверные запросы

Тяни данные **минимумом отчётов** (`report/`), а агрегации и счётчики считай **на
клиенте**. Фронт-вычисления дёшевы и никого не нагружают; разовая трудоёмкость
кода окупается годами сниженной нагрузки на сервер. Если виджету не хватает поля —
добавь колонку в существующий отчёт, а не заводи лишний запрос. Считай `*_id`
(функция abn_ID) и дедуплицируй строки на клиенте вместо отдельных выгрузок.

Пример: рабочее место «Дашборды» (`download/atex/js/dashboards.js`) заменило ~10
запросов (6 выгрузок `object/` + 4 `_count`) на **2 отчёта** (`order_pipeline`,
`material_stock`); все 5 виджетов агрегируются на клиенте из строк (dedup по `*_id`).

## 3. Соглашение об именах (правило проекта)

Имя колонки служит сразу **ключом в `JSON_KV`** и **именем внешнего фильтра
`FR_/TO_` в URL**, поэтому имена — латиница, `snake_case`, без пробелов.

- **Отчёт (поле 22):** `<область>_<суть>` — напр. `order_pipeline`. Без префикса
  базы (внутри одной БД он у всех одинаков и ничего не различает).
  Имя используется и в URL (`report/{имя}`). Переименовать готовый запрос —
  `_m_save/{queryId}?JSON t22=новое_имя` (именно `_m_save` и `t22` — id типа).
- **Колонка (`t100`):** `<сущность>_<поле>`. Префиксы сущностей:
  `order_`, `position_`, `provision_`, `cut_`, `gp_`. ID-колонка — `<сущность>_id`.
  Единицы измерения суффиксом: `_mm`, `_m`. Примеры: `order_id`, `order_status`,
  `position_width_mm`, `provision_used_m`, `gp_arrived`.
- **Почему латиница/snake_case:** исключает URL-кодирование кириллицы и подмену
  пробелов на `_` (ядро делает это для `t100` с пробелами); префикс сущности
  снимает коллизии имён в join-отчёте.

## 4. Пилот: `order_pipeline` (queryId 8303)

Создан в боевой ateh. Цепочка **Заказ → Позиция → Обеспечение (расход) → Резка → Партия ГП**.

| Колонка (`t100`) | Источник `t28` | Функция |
|---|---|---|
| order_id | 1075 Заказ | abn_ID (t104=85) |
| order_no | 1075 Заказ (Номер) | — |
| order_status | 1131 | — |
| position_id | 1076 Позиция | abn_ID |
| position_cut_type | 1140 Тип резки (ref) | — |
| position_cut_type_id | 1140 (ref) | abn_ID → ID справочного значения |
| position_width_mm | 1141 | — |
| provision_id | 1077 Обеспечение | abn_ID |
| provision_used_m | 1149 Метраж (расход) | — |
| provision_status | 1154 | — |
| cut_id | 1078 Резка | abn_ID |
| cut_no | 1078 Резка (Номер) | — |
| cut_status | 1162 | — |
| gp_id | 1081 Партия ГП | abn_ID |
| gp_arrived | 1081 Дата прихода | формат `t84=DATETIME` |
| gp_fact | 1081 «Кол-во факт» (70573) | — (#3433: фактически произведённые рулоны) |
| gp_plan | 1081 «Кол-во план» (70575) | — (#3433: план = полосы × проходов) |
| gp_order_id | 1081 «ID заказа» (70577) | — (#3433: заказ партии; пусто = запас) |

> **#3433 (серверная доделка отчёта):** колонки `gp_fact`/`gp_plan`/`gp_order_id`
> добавить в `order_pipeline` на сервере. Дашборд «Выпуск ГП» (`dashboards.js`
> `rowsToEntities`) показывает фактически выпущенные рулоны из `gp_fact`; пока колонки
> нет — фолбэк на `gp_rolls` (старое поведение). `gp_rolls` после #3433 = СПРОС, не выпуск.

Запуск: `GET /ateh/report/8303?JSON_KV`. Незавершённые стадии = пустые поля (LEFT JOIN).
Пример полностью пройденной строки:

```json
{
  "order_no": "АТХ-3002-2026-05-31", "order_status": "Выполнен",
  "position_cut_type": "TT-АТХ-3002-2026-05-31 57x10+40x8", "position_width_mm": "57.00",
  "provision_used_m": "1200.00", "provision_status": "Выполнено",
  "cut_no": "4", "cut_status": "Завершён", "gp_arrived": "1780235295",
  "order_id": "1966", "position_id": "1974", "position_cut_type_id": "1951",
  "provision_id": "1993", "cut_id": "1982", "gp_id": "2012"
}
```

## 5. Как создать отчёт (проверенная последовательность)

```bash
DB=https://ideav.ru/ateh; TOKEN=<актуальный>
XSRF=$(curl -s -H "X-Authorization: $TOKEN" "$DB/xsrf?JSON=1" | jq -r ._xsrf)

# 1. Запрос (тип 22). up=1 ОБЯЗАТЕЛЕН; имя — slug по соглашению.
curl -s "$DB/_m_new/22?JSON&up=1" --data-urlencode "t22=order_pipeline" \
  --data-urlencode "token=$TOKEN" --data-urlencode "_xsrf=$XSRF"   # → {"id": <queryId>}

# 2. Колонка (тип 28). up=queryId; t28 = ID таблицы ИЛИ реквизита; t100 = имя.
#    Порядок колонок = порядок join слева-направо; первая реальная колонка — мастер-таблица.
curl -s "$DB/_m_new/28?JSON&up=<queryId>" --data-urlencode "t28=<fieldId>" \
  --data-urlencode "t100=order_status" \
  --data-urlencode "token=$TOKEN" --data-urlencode "_xsrf=$XSRF"   # → {"id": <colId>}

# 3. ID сущности — функция abn_ID. «Функция» (t104) — это ССЫЛКА; значение = 85
#    (НЕ строка "abn_ID"). Для ref-колонки abn_ID даёт ID справочного значения.
curl -s "$DB/_m_set/<colId>?JSON" --data-urlencode "t104=85" \
  --data-urlencode "token=$TOKEN" --data-urlencode "_xsrf=$XSRF"

# 4. Формат отображения (опц.): _m_set/<colId> t84=DATETIME|DATE|NUMBER|…
```

**Фильтрация при запуске** (только по колонкам с заданным `t100`):

```
GET /{db}/report/{id}?JSON_KV&FR_{колонка}={значение}     # нижняя граница (FROM)
GET /{db}/report/{id}?JSON_KV&TO_{колонка}={значение}     # верхняя граница (TO)
# напр.: ?JSON_KV&FR_provision_status=Выполнено
#        ?JSON_KV&FR_cut_id=1900&TO_cut_id=2000           # диапазон по числовому ID
```

**⚠️ Открытый интервал — нужны операторы `>` / `<`** (проверено на бою, отчёт
`orders_list` по дате):

- **Обе границы заданы → диапазон без операторов:** `FR_{колонка}={С}&TO_{колонка}={По}`.
- **Только нижняя граница (открыт сверху) → оператор `>`:** `FR_{колонка}=>{С}`.
- **Только верхняя граница (открыт снизу) → оператор `<`:** `TO_{колонка}=<{По}`.

Одиночные `FR_{колонка}={значение}` / `TO_{колонка}={значение}` БЕЗ оператора
трактуются как **точное совпадение** (для дат/чисел открытого интервала это даёт
0 строк; для текстового статуса exact-совпадение — рабочее поведение).

```
# напр.: ?JSON_KV&FR_order_created=>2026-05-01                 # с 1 мая и позже
#        ?JSON_KV&TO_order_created=<2026-06-01                 # до 1 июня
#        ?JSON_KV&FR_order_created=2026-05-01&TO_order_created=2026-05-31  # май (диапазон)
```

ID компактнее символьных значений и удобнее для фильтра — поэтому для отбора по
сущности кладём `<сущность>_id` (abn_ID), а символьные значения — только если
нужны для показа.

Полный справочник полей колонки (тип 28: `t100`, `t58` ALIAS, `t84` формат,
`t101` формула, `t104` функция, агрегации, сортировка `t109`, фильтры `t102/t103`)
— `docs/integram-app-workflow.md`, раздел «Создание запроса программно».

## 6. Найденные грабли

- **`up=1` обязателен** при создании запроса (тип 22), иначе ошибка `up=0`.
- **`abn_ID` — это ссылка на функцию, значение `t104=85`** (не строка «abn_ID»;
  строка молча не сохраняется).
- **`JSON_KV` отдаёт СЫРЫЕ значения** — формат из `t84` виден только в
  платформенном `report/{id}?JSON` (поле `format`). DATETIME-штамп (`gp_arrived`)
  форматировать **на клиенте** через `AtexRefSearch.formatDateTime`.
- ⚠️ **Не форматировать дату формулой `t101=FROM_UNIXTIME([THIS],…)`** — ломает
  join (отчёт схлопывается в одну строку).
- **Переименование отчёта** — имя это главное значение записи (тип 22), поэтому
  меняется через `_m_save/{queryId}?JSON t22=новое_имя` (именно `_m_save`, не
  `_m_set`; параметр `t22` — id типа, не `t{recordId}`; `up=1` не нужен).

## 7. `preferable_widths` (queryId 8421) — ходовые ширины по сырью

Отчёт для РМ «Расчёт резки» (ideav/atex#52, подзадача B): какие ширины ролика и в
каком суммарном количестве заказывают по каждому виду сырья — чтобы добирать остаток
джамбо «ходовыми» ширинами. Над «Позиция заказа», группировка по ширине, сырью,
типу намотки и длине, `SUM(Кол-во)` по убыванию, внешние фильтры по виду сырья,
типу намотки и длине.

| Колонка (`t100`) | Источник (`t28`, реквизит Позиции заказа) | Функция (`t104`) | Сортировка (`t109`) |
|---|---|---|---|
| `position_width_mm` | 1141 Ширина, мм | — (ключ группировки, для показа) | — |
| `position_width_id` | 1141 Ширина, мм | 85 (abn_ID) | — |
| `position_material_id` | 1138 Вид сырья | 85 (abn_ID) | — (внешний фильтр `FR_`) |
| `wind_dir` | 8463 Тип намотки | — | — (внешний фильтр `FR_`) |
| `wind_length` | 1143 Длина, м | — | — (внешний фильтр `FR_`) |
| `position_qty_sum` | 1137 Кол-во | 73 (SUM) | −1 (по убыванию) |

Имена — latin `snake_case`, поэтому `FR_position_material_id`, `FR_wind_dir` и
`FR_wind_length` в URL не требуют кодирования кириллицы.

Создание (проверенная последовательность, `DB=https://ideav.ru/ateh`):
```bash
XSRF=$(curl -s -H "X-Authorization: $TOKEN" --cookie "idb_ateh=$TOKEN" "$DB/xsrf?JSON=1" | jq -r ._xsrf)
# Запрос (тип 22), up=1
QID=$(curl -s -H "X-Authorization: $TOKEN" --cookie "idb_ateh=$TOKEN" "$DB/_m_new/22?JSON&up=1" \
  --data-urlencode t22=preferable_widths --data-urlencode token=$TOKEN --data-urlencode _xsrf=$XSRF | jq -r '.id // .obj')
# Колонки (тип 28), up=QID: t28=реквизит, t100=имя
#   position_width_mm←1141, position_width_id←1141, position_material_id←1138,
#   wind_dir←8463, wind_length←1143, position_qty_sum←1137
# Функции/сортировка через _m_set: t104=85 (abn_ID) для *_id; t104=73 (SUM) + t109=-1 для position_qty_sum
```
Запуск:
`GET /ateh/report/preferable_widths?JSON_KV&FR_position_material_id={видСырьяID}&FR_wind_dir={IN|OUT}&FR_wind_length={длина}`.
`{видСырьяID}` — ID записи «Вид сырья» (= `position_material_id`/abn_ID из ответа),
`{длина}` — требуемая длина позиции в метрах. Незаданный фильтр расширяет выборку.
Клиент `production-planning.js` дополнительно отбрасывает строки с другим `wind_dir`
или `wind_length`, чтобы не использовать ходовые другого профиля. Создано и проверено
2026-06-01; профильные колонки добавлены для #3219/#3221 (queryId 8421).

## 8. `cut_planning` (queryId 8384) — очередь резок с сигналами движка

Отчёт для РМ «Планирование производства» и движка упорядочивания D2 (ideav/crm#52,
подзадача D). Цепочка **Производственная резка → Обеспечение → Позиция заказа** плюс
дополнительные колонки сигналов (добавлены в рамках D3a), которые `rowsToPlanning`
разворачивает в дескриптор для `orderCuts`/`planQueues`.

Базовые колонки (`cut_id`, `cut_no`, `cut_slitter_id`, `cut_slitter`, `cut_status`,
`cut_sequence`, `supply_id`, `supply_position_id`) — исходные. Ниже — 7 новых
сигнальных колонок.

| Колонка (`t100`) | Источник (`t28`) | Функция | Назначение в движке |
|---|---|---|---|
| `cut_material_id` | Резка → «Вид сырья» (95358, ref→1069) | 85 (abn_ID) | ключ группировки по сырью в `changeoverCost` |
| `cut_material` | Резка → «Вид сырья» (95358, name) | — | определение Фольги (`/фольг/i`) → `isFoil`; флаг отправляет резку в конец очереди |
| `cut_batch_id` | Резка → Партия сырья (abn_ID) | 85 (abn_ID) | ключ смены партии; штраф `remainder` при неудобном остатке |
| `cut_jumbo_remaining` | Партия сырья → «Остаток, м» | — | `jumboRemainingM` (число, метры); входит в `awkwardRemainder` |
| ~~`cut_knives`~~ | ~~Тип резки → «Итого ножей»~~ | — | **УДАЛЕНА в F2** (Тип резки упразднён). `knifeCount` теперь считается клиентом из подчинённых «Полос» резки (SUM Количество) — см. F3. |
| `cut_winding` | Обеспечение → Позиция → «Тип намотки» | — | нормализуется `normWinding` → `'IN'`/`'OUT'`/`''`; вес `winding` в `changeoverCost` |
| `cut_roller_width` | Позиция → «Ширина, мм» | — | `rollerWidth` (число, мм); штраф за сужение вала в `changeoverCost` |
| `order_id` | Позиция → Заказ (abn_ID, t104=85) | 85 | id заказа резки (для группировки/отладки) |
| `order_approval_date` | Позиция → Заказ → «Дата согласования» (1130) | — | фильтр видимости очереди: показываем только резки согласованных заказов (`isCutVisible`) |
| `cut_knife_setup_min` | Резка → «Наладка ножей, мин» (96067) | — | #3698: хранимая наладка ножей (`KNIFE`), мин; Гант берёт готовое значение вместо пересчёта |
| `cut_material_winding_min` | Резка → «Сырье/намотка, мин» (96069) | — | #3698: хранимая смена сырья/намотки (`MATERIAL_WINDING`), мин; Гант берёт готовое значение вместо пересчёта |
| `cut_time` | Резка → «Резка и Лидер» (96778) | — | #3700: хранимое «намотка + лидер», мин; Гант рисует сегмент резки по нему (запланированные), вместо грубого окна |
| `cut_runs_fact` | Резка → «Кол-во резок факт» (657315) | — | #4564: СДЕЛАНО проходов. «Урегулировать» этим числом разделяет частично выполненное задание: сделанные проходы остаются исходной записью в её фактическом дне, остаток уезжает новой записью в план |

> **#4564:** `cut_runs_fact` («Кол-во резок факт», реквизит 657315 таблицы 1078) нужна ДВУМ
> отчётам: `cut_planning` (queryId 8384) — «Планирование производства», Гант резки и календарь
> станка; `slitter_cuts` (queryId 80981) — пульт слиттера (его спека — в
> `docs/scripts/create_slitter_reports.py`). Число пишет пульт при каждой отметке ✓ Готово
> (`markPassDone`), читают заголовок «Резка N из M» и «Урегулировать». НЕТ колонки — начатые просроченные задания
> не разделяются и не двигаются вовсе, и РМ об этом говорит (молчаливый ноль означал бы «сделано
> ничего» и увёз бы со дня работу, которая идёт на станке).

> **#4606 (номер заказа на пульте):** `cut_planning` — источник колонки `order_no` (номер
> заказа, главное значение 1075 через Позицию) для двух РМ: Гант резки (`cut-gantt`, подпись
> строки) и пульт слиттера (`slitter.js`, `loadCutOrders` — крупный номер в карточке очереди и
> в шапке текущего задания). Пульт зовёт отчёт с фильтром станка
> (`FR_cut_slitter_id`) и кэширует ответ на выбранный станок. Строк на задание столько, сколько
> у него обеспечений, поэтому номера схлопываются по `cut_id` (`core.rowsToCutOrders`), а
> несколько заказов одного задания показываются через запятую. Отчёт не ответил или не отдал
> `order_no` — карточки остаются без номера, остальное РМ работает как раньше.
>
> **Проверено на боевой ateh 04.08.2026:** `report/cut_planning?JSON_KV&FR_cut_slitter_id=1277`
> — 76 строк, ровно один станок в выдаче, `order_no` заполнен во всех строках (номера короткие:
> «244», «4276»), заданий с несколькими заказами в выборке нет. В `slitter_cuts` колонки с
> заказом действительно нет (24 колонки, ни одной `*order*`) — отсюда джойн через `cut_planning`,
> а не расширение своего отчёта. Доступ роли **Оператор** (1621): READ на объект «Запрос» (22) и
> на всю цепочку — 1075 «Заказ», 1076 «Заказанное количество», 1077 «Обеспечение», 1078 «Задание
> в производство»; грантов на *конкретные* отчёты в базе нет ни у одной роли, поэтому
> `cut_planning` читается тем же грантом, что и уже работающий `slitter_cuts`.

> **#3698/#3700:** три колонки выше (`cut_knife_setup_min`, `cut_material_winding_min`,
> `cut_time`) нужно добавить в отчёт `cut_planning` (queryId 8384) на сервере (`t28` =
> реквизиты 96067 / 96069 / 96778 таблицы «Задание в производство»). Значения пишет РМ
> «Планирование производства» (`persistCutSetupColumns`) после любого сохранения
> порядка/плана; алиасы читаются как `cut_knife_setup_min` / `cut_material_winding_min` /
> `cut_time` (см. `CUT_*_COLUMNS` в обоих РМ).

> Колонки `order_id` (8568) и `order_approval_date` (8566) добавлены для фильтра
> видимости очереди (`isCutVisible`): движок отчёта дожойнивает Позиция→Заказ
> (Позиция — подчинённая таблица Заказа).

> **#3868 (серверная правка отчёта):** `cut_material`/`cut_material_id` (colId **8474**/
> **8484**) ПЕРЕНАСТРОЕНЫ с источника «Партия сырья → Вид сырья» (поле 1117 на табл. 1074)
> на СОБСТВЕННОЕ «Вид сырья» резки (req **95358**, табл. 1078, ref→1069) через
> `_m_save/{colId}?JSON t28=95358`. Причина: «связанные события»/копия задания теряют
> «Партию сырья», но «Вид сырья» на резке задано корректно (его пишет планировщик, #3688) →
> прежний источник отдавал пустой материал, и Гант (`cut-gantt`, read-only) рисовал первую
> резку дня без сырья. Замена безопасна: на боевой ateh из 188 резок, где заданы оба пути,
> 0 расхождений и 0 случаев «материал только в Партии сырья» (нулевая регрессия), 12 пустых
> вылечены. Главное значение колонки (источник `t28`) пишется только `_m_save`, не `_m_set`
> (иначе 403). **Применено на ateh; на atex (prod) выполнить ту же правку** (id колонок и req
> «Вид сырья» подтвердить через `report/cut_planning?JSON&LIMIT=0,1` и `obj_meta/{cutTable}?JSON`).

> **#4128 (та же болезнь у намотки):** `cut_winding` идёт цепочкой Обеспечение → Позиция,
> но обеспечения записи-ПРОДОЛЖЕНИЯ дробления по дням создаются `up = positionId` и БЕЗ
> реквизита «Задание в производство» (см. `applySplitPlan`, шаг 2b), а у setup-сегмента
> (0 проходов) доли обеспечений обнулены. Поэтому у звеньев цепочки дробления колонка
> отдаёт пустоту, хотя собственный «Тип намотки» (req **28144**, табл. 1078) на резке
> задан планировщиком. Пустота ещё и копировалась в новые продолжения → намотка терялась
> насовсем (IN/OUT пропадали с карточки очереди и из «задания на наладку»).
> Клиент (`production-planning`) с #4128 читает намотку из СОБСТВЕННОГО реквизита резки
> (`loadCutSequences`, тот же `object/{cutTable}`, что и «Зафиксировано»), а колонку отчёта
> держит фолбэком. Read-only потребители отчёта (`cut-gantt.js`, `slitter.js` — список резок)
> по-прежнему зависят от колонки: **на ateh и atex переключить источник `cut_winding` на
> `t28=28144`** тем же способом, что и `cut_material` в #3868 (`_m_save/{colId}?JSON`);
> colId подтвердить через `report/cut_planning?JSON&LIMIT=0,1` и `obj_meta/{cutTable}?JSON`.

Запуск: `GET /ateh/report/cut_planning?JSON_KV&LIMIT=0,5000`. Строки разворачиваются
`rowsToPlanning` в `{ cuts, supplies }` (dedup резок по `cut_id`); дескриптор каждой
резки содержит все 7 полей и передаётся напрямую в `orderCuts`/`planQueues`.

> **#3675 п.3:** минуты наладки ПЕРЕД резкой Гант (`cut-gantt`) считает САМ — отчёт их не
> отдаёт (это переналадка с предыдущей резки станка, зависит от соседа и не хранится). Гант
> догружает `cut_strips` (раскладка ножей) и таблицу «Время операции, мин» (op-времена) и
> повторяет `changeoverParts` планировщика: `attachSetupMinutes` пишет на каждое задание
> `setupKnifeMin` (смена набора ножей / сужение ролика, `KNIFE`) и `setupMaterialMin`
> (смена сырья / намотки, `MATERIAL_WINDING`). Партию сырья не сравниваем (нет в `cut_planning`).
> Сегменты рисуются перед резкой: наладка ножей светлее, смена сырья чуть темнее, резка — главным цветом.

> **#3698:** если отчёт отдаёт `cut_knife_setup_min` / `cut_material_winding_min`, Гант
> ПРЕДПОЧИТАЕТ эти хранимые минуты пересчёту (`attachSetupMinutes`): «Планирование
> производства» считает их с учётом смены партии сырья (которой нет в `cut_planning`) и
> сохраняет в задание — это устраняет расхождение «та же намотка, другая партия». Пересчёт
> по соседям остаётся фолбэком для резок без сохранённых значений (легаси/до миграции).

> **#3700:** если отчёт отдаёт `cut_time` («Резка и Лидер» = намотка + лидер), Гант рисует
> сегмент резки запланированного задания по этим минутам (`cutBarMinutes` → `cutBarSegments`
> / `ganttWindow`), а не по грубому окну `cutTimeRange` (план: дедлайн или +30 мин). Начатые/
> завершённые задания по-прежнему по факту; нет `cut_time` → прежнее окно (фолбэк). Тултип
> бара показывает «Резка и лидер: N мин».

## 8.1 `positions_list` (queryId 8409) — позиции заказов (+ сырьё)

Отчёт-источник позиций для РМ «Планирование производства». Используется в D3b для
генерации резок под необеспеченные позиции (`generateCutPlan`): из строк
`rowsToGenPositions` собирает дескрипторы `{ id, materialId, width, qty, length, dueKey }`.

| Колонка (`t100`) | Источник (`t28`) | Функция |
|---|---|---|
| `position_id` | 1076 Позиция | abn_ID (t104=85) |
| `position_no` | 1076 Позиция (Номер) | — |
| ~~`position_cut_type`~~ | ~~1140 Тип резки~~ | **УДАЛЕНА в F2** (Тип резки упразднён, реквизит 1140 снят) |
| `position_width` | 1141 Ширина, мм | — |
| `position_qty` | 1137 Кол-во | — |
| `position_material_id` | 1138 Вид сырья | 85 (abn_ID) |
| `position_due_date` | 8627 Срок изготовления | — (F3: дата-окно объединения, `dueKey`) |
| `position_length` | 1143 Длина, м | — (#3155: метраж прогона джамбо, `length`) |
| `position_winding` | 8463 Тип намотки | — (`windDir`) |
| `order_id` | Позиция → Заказ (abn_ID, t104=85) | id заказа позиции (#3433, «ID заказа» «Партии ГП») |

`position_material_id` (колонка 8527) — сырьё позиции; `position_due_date` (8654, F3) —
«Срок изготовления» для окна объединения позиций в одну резку. `position_length`
(реквизит 1143 «Длина, м» позиции, #3155) — длина прогона джамбо: при генерации резок
её записывают в «Метраж, м» создаваемого обеспечения, иначе `footageBySupply=0` и все
резки показывают «0 мин» (намотка не считается). `position_winding` вместе с
`position_length` образует профиль планирования для группировки и запроса
`preferable_widths`. `rowsToGenPositions` собирает
`{ id, materialId, width, qty, length, windDir, windLength, dueKey, orderId }` для ядра
`cut-layout.planLayouts`.
Запуск: `GET /ateh/report/positions_list?JSON_KV`.

> **#3433 (серверная доделка отчёта):** колонку `order_id` (Позиция → Заказ, `abn_ID`,
> `t104=85`) нужно добавить в `positions_list` на сервере (как у `cut_planning`/
> `order_pipeline`). Пока её нет — `rowsToGenPositions` читает пусто, и «ID заказа»
> создаваемой «Партии ГП» остаётся пустым (трактуется как «в запас»). Клиент к этому
> устойчив; функциональность «ID заказа» включится после добавления колонки.

## 8.2 `cut_strips` (queryId 8656, F3) — полосы (ножи) резок

Резка → подчинённые «Полоса». Нужен production-planning, чтобы восстановить
`knifeCount`/`knifeWidths` для движка очереди (`changeoverCost`), т.к. `cut_knives`
из `cut_planning` убран в F2. `aggregateStrips(rows)` сворачивает в
`{cutId: {knifeCount: Σ strip_qty, knifeWidths: [width×qty…]}}`.

| Колонка (`t100`) | Источник (`t28`) | Функция |
|---|---|---|
| `cut_id` | 1078 Производственная резка | 85 (abn_ID) |
| `strip_width` | «Партия ГП» «Ширина, мм» (1186) | — |
| `strip_qty` | «Партия ГП» «Кол-во полос» (70190) | — |

Запуск: `GET /ateh/report/cut_strips?JSON_KV`.

**#3431:** `strip_qty` берётся из «Кол-во полос» (полос за проход) — это число ножей,
геометрия раскроя. При смене смысла колонок отчёт `cut_strips` (queryId 8656) на сервере
перенастроен на «Кол-во полос»; иначе `knifeCount` раздувается в × проходов.

**#3433:** в «Партии ГП» (1081) рулоны разделены на план/спрос/факт, поэтому `cut_strips`
по-прежнему обязан читать «Кол-во полос» (70190), не рулоны:
- **«Кол-во рулонов»** (1188) — СПРОС: рулоны связанного «Обеспечения» (под заказ);
- **«Кол-во план»** (70575) — план производства = полосы × число резок (проходов);
- **«Кол-во факт»** (70573) — фактически произведённые рулоны (пишутся в производстве —
  «Пульт слиттера» при завершении — и на «Складе ГП» при оприходовании);
- **«ID заказа»** (70577) — заказ партии (пусто = в запас).

Клиент: `js/integram-table`-нет; рабочие места `download/atex/js/production-planning.js`
(создание, `FINISHED_BATCH_REQ`), `warehouse.js` (факт), `slitter.js` (факт),
`dashboards.js` («Выпуск ГП» по факту).

## 8.3 Конфигурация резок станка: `next_cut_setup` (643132) и `prev_cut_setup` (93371)

Два запроса одинакового состава и с ПРОТИВОПОЛОЖНОЙ сортировкой `task_start`. Одним оба
сценария не закрыть: клиент читает ответ страницей (`LIMIT` из определения, см. ниже), поэтому
направление сортировки решает, какой край выборки до него доедет.

| Запрос | Сорт. `task_start` | Отвечает на вопрос | Кто читает |
|---|---|---|---|
| `next_cut_setup` (643132) | ↑ (`Сорт.` 1), «до» = `31/12/2099` | ближайшая резка **после** границы | `slitter.js` — «оставить конфигурацию» (#3737) |
| `prev_cut_setup` (93371) | ↓ (`Сорт.` −1) | последняя резка **до** границы | — (планировщик и Гант отвязаны, #4371) |

Строка — на «Партию ГП»: ширина полосы, количество полос, сырьё, намотка.

| Колонка | Источник | Назначение |
|---|---|---|
| `task_start` | «Дата план» резки (1078, DATETIME) | порядок расписания + граница дня |
| `slitter_id` | «Слиттер» резки (`abn_ID`) | фильтр `FR_slitter_id` |
| `task_id` | 1078 Производственная резка (`abn_ID`) | группировка полос в резку |
| `batch_ord` | порядок «Партии ГП» в резке (`abn_ORD`, `Сорт.` 2) | порядок полос |
| `width` | «Партия ГП» «Ширина, мм» (1186) | набор ножей |
| `batch_count` | «Партия ГП» «Кол-во полос» (70190) | сколько ножей этой ширины |
| `material_id` | «Вид сырья» резки (95358, `abn_ID`) | сравнение сырья |
| `wind_dir` | «Намотка» резки (28144) | IN/OUT |

**Умолчания, зашитые в определения** (сверено на ateh1 24.07.2026) — их надо перебивать
параметрами запроса, иначе получите не то, что ждёте:

- `task_start`: «Значение (от)» = `[NOW]` — **без своего `FR_task_start` отчёт отдаёт только
  будущее**. `FR_task_start` перекрывает «от», `TO_task_start` — «до».
- `slitter_id`: «Значение (от)» = `1279` — **без своего `FR_slitter_id` отчёт отдаёт один
  станок**, а не все.
- `LIMIT` = `100` в определении. Клиентский `LIMIT` не расширяет его, а **сужает**: ядро берёт
  `min(запрошенное, заданное)` (`index.php:3770`), поэтому `LIMIT=0,2000` превращается в
  `LIMIT 0,100`. Обрезается ДАЛЬНИЙ от сортировки край, и обрезаться может середина резки —
  тогда набор ширин приедет неполным, и сравнение «те же ножи» даст ложное «нет».

Запуск (первая резка следующего дня для станка): `GET /ateh1/report/next_cut_setup?JSON_KV&FR_slitter_id={станок}&FR_task_start=>{штамп
полуночи дня}` — пример: `…&FR_task_start=%3E1784840400&FR_slitter_id=1282` → 11 строк,
первая резка 27.07 совпадает по ножам (110×8) и сырью с последней резкой 24.07.
Клиент: `download/atex/js/slitter.js` (`computeSeamless` → `core.nextDaySetupConfig`).

**Базы разошлись:** `next_cut_setup` заведён только на `ateh1` (37 запросов против 36 на `ateh`),
и там же у `prev_cut_setup` сортировка `task_start` ↓; на `ateh` она ↑ со «до» = `31/12/2099`.
`slitter.js` один на все базы, поэтому на `ateh` подсказка смены не появляется — запрос надо
завести и там (issue #4371).

## 9. Следующий шаг

Переключить **dashboards** на `report/8303` вместо 6 клиентских выгрузок —
максимальная выгода и ровно случай «Заказы-Позиции-расход». Это отдельная задача.

## 10. SET-действия (запрос как триггер: «Согласовать», смена статуса)

Отчёт может не только читать, но и **изменять** данные: у колонки задаётся
SET-выражение (поле `t132`), которое применяется ко всем записям под фильтром —
**только при `confirmed=1`**. Без `confirmed` отчёт возвращает превью затрагиваемых
строк.

```
# Превью (что изменится):
GET  /{db}/report/{name}?JSON_KV&FR_{колонка}={id}
# Применить:
POST /{db}/report/{name}?JSON_KV     Body: confirmed=1&FR_{колонка}={id}&_xsrf=...
```

SET-выражение (`t132`): литералы в кавычках (`"Согласован"`), даты — встроенными
значениями `[TODAY]`/`[NOW]` (см. список в §«Формулы»). Создание SET-колонки:
`_m_new/28?up={qid} t28={источник} t100={имя} t132={выражение}`.

**⭐ Запрос выполняется с правами ВЛАДЕЛЬЦА**, поэтому SET-отчёт меняет поле, даже
если оно **READ** для текущей роли. Это штатный приём: поле делают read-only для
роли (чтобы не правили вручную), а изменение идёт **только** через кнопку →
SET-отчёт с `confirmed=1`.

**Понизить отдельное поле до READ** (при WRITE на таблицу): добавить роли грант на
**колонку** (reqId), а не таблицу — запись в таблице 116:
`POST _m_new/116?up={roleId} t116={reqId} t136=53` (53=READ). Уровень таблицы
наследуется на колонки, грант на колонку понижает конкретное поле.

**Пример (ateh, РМ orders, 2026-06-02):** кнопка «Согласовано» вызывает отчёты
`approve_order` (`FR_order_id`) / `approve_position` (`FR_position_id`) — SET
ставит Статус=«Согласован/Согласована» + Дата согласования=`[TODAY]`. Поля Статус и
Дата согласования у роли Менеджер — READ (гранты на reqId в таблице 116), поэтому
менеджер меняет их только кнопкой (SET под правами владельца), но не вручную.

## 11. `packer` (queryId 673812) — что упаковщику упаковывать

Отчёт для РМ «Упаковка» (`/atex/packer`, #4658). Цепочка **Задание в производство →
Партия ГП → Обеспечение → Заказанное количество → Заказ**; строка отчёта — одна
**Партия ГП** внутри задания, то есть ровно то, что упаковщик берёт в руки.

| Колонка (`t100`) | Источник (`t28`) | Функция (`t104`) | Зачем |
|---|---|---|---|
| `task` | 1078 Задание в производство (главное значение) | — (сорт. `t109=1`) | плановый старт, Unix |
| `task_id` | 1078 Задание в производство | 85 (abn_ID) | ссылка события «Упаковка» на задание |
| `gp_id` | 1081 Партия ГП | 85 (abn_ID) | адрес записи отметки (`_m_set`) |
| `order_no` | 1075 Заказ (главное значение) | — | номер заказа (крупно в карточке) |
| `order` | 673841 Заказ → Заказ клиента | — | номер заказа у клиента (может быть пуст) |
| `material` | 95358 Задание → Вид сырья | — | подпись ролика |
| `cut_width` | 1141 Позиция → Ширина, мм | — | подпись ролика |
| `cut_length` | 1143 Позиция → Длина, м | — | подпись ролика |
| `wind_direction` | 8463 Позиция → Тип намотки | — | IN/OUT |
| `sleeve` | 8194 Позиция → Диаметр втулки | — | какая втулка |
| `add_sleeve` | 66418 Позиция → Доп. втулка | — | «Приклеить»/«Вложить»/… |
| `qty` | 1188 Партия ГП → Кол-во рулонов | — | план |
| `qty_fact` | 70573 Партия ГП → Кол-во факт | — | факт (он и предлагается к упаковке) |
| `packed` | 673786 Партия ГП → Упаковано шт | — | состояние упаковки |
| `notes` | 673789 Партия ГП → Примечание | — | почему количество поправили |
| `events` | 16419 Событие смены → Тип события | 74 (COUNT) | сколько событий у задания |
| `packer_no` | 669271 Слиттер → Упаковочное место | — | **внешний фильтр `FR_packer_no`** |
| *(скрытая)* | 16419 Событие смены → Тип события | 85 (abn_ID), «Значение (от)» `77054` | **фильтр: у задания есть событие «Резка»** |
| *(скрытые)* | 1077 Обеспечение, 1076 Заказанное количество | — | звенья join |

Скрытая колонка «Тип события» с фильтром `77054` («Резка») и даёт правило РМ: задание
появляется у упаковщика, **как только по нему сделана первая резка**, и сразу всем своим
объёмом. Скрытые колонки (`t107=X`) в `JSON_KV` не выводятся — они только join и фильтр.

Запуск: `GET /ateh/report/packer?JSON_KV&LIMIT=0,5000&FR_packer_no={номер места}` —
упаковщик получает только позиции своего упаковочного места (#4681). Значение фильтра —
**главное значение** записи «Упаковочное место», то есть её номер, а не id записи;
`FR_` работает по «Имени в отчёте» (`t100`) колонки.

**Упаковочное место — свойство станка**, а не позиции: цепочка `Задание в производство
(1078) → Слиттер (1156 → 1070) → Упаковочное место (669271 → 669269)`. Позиция достаётся
тому месту, к которому привязан станок, режущий задание. Отсюда правило эксплуатации:
**у каждого слиттера место должно быть проставлено** — у станка без места позиции не
попадут ни в один список упаковщиков, потому что `FR_packer_no` не совпадёт ни с одним
номером. На ateh: Станки 1 и 2 → место «1», Станки 3 и 4 → место «2».

Отметка упаковки пишется мимо
отчёта: `_m_set/{gp_id}` («Упаковано шт», «Примечание») и `_m_new/1082?up=1` — событие
смены с типом «Упаковка» (справочник «Тип события» 1193, запись 670935) и ссылкой на
задание. Ссылочный «Тип события» принимает **имя значения** (`t16419=Упаковка`) и
сохраняется как `670935:Упаковка`.

## 12. `pack_sizes` (queryId 674477) — справочник типоразмеров упаковки

Справочник «Типоразмер» (670936) целиком: в какой короб и по сколько штук укладывать ролик.
Читают РМ «Планирование производства» и «Упаковка» (#4665). Отчёт идёт **под правами
владельца**, поэтому ролям Диспетчера и Оператора грант на сам справочник не нужен.

| Колонка (`t100`) | Источник (`t28`) | Функция |
|---|---|---|
| `size_id` / `size_name` | 670936 Типоразмер | 85 (abn_ID) / — |
| `add_sleeve` | 670937 Доп. втулка | — |
| `rows_cnt` / `per_row` / `per_box` | 670939 / 670941 / 670943 | — |
| `box` | 670946 № короба | — |
| `w_from` / `w_to` | 673794 Втулка от / 673796 Втулка до | — |
| `l_from` / `l_to` | 673798 Длина от / 673800 Длина до | — |
| `foil` | 673802 Фольга | — |

Диапазоны **смежные, без дыр**: ширина ролика `0–20`, `21–24`, `25–30`, `31–40`, `41–61`,
`62–83`, `84–124`, `125–165`, `166–220`; длина намотки `0–200`, `201–320`, `321–450`,
`451–540`, `541–600`, `601–700`, `701–800`, `801–1100`, `1101–1300`. У фольги длины
**точные** (122, 183, 244, 305, 700). Шесть строк, привязанных к типу втулки
(`110х74/110 втулка 1"`, `втулка 0,5" 57 мм`, `втулка 0,5" 110 мм`), диапазонов не имеют
и в автоподбор не идут. Подбор — `download/atex/js/packaging-size.js`.

## 13. `gp_pack` (queryId 674405) — Партии ГП с параметрами упаковки

Служебный отчёт для массовой простановки «Типоразмера» (#4665): «Партия ГП» плоско не
читается (подчинённая таблица, `object/1081` без `F_U` отдаёт 0 строк).

| Колонка | Источник | Функция |
|---|---|---|
| `task_id` / `gp_id` | 1078 Задание / 1081 Партия ГП | 85 (abn_ID) |
| `gp_width` | 1186 Партия ГП → Ширина, мм | — |
| `tipo` / `tipo_id` | 673788 Партия ГП → Типоразмер | — / 85 |
| `length` | 1143 Позиция → Длина, м | — |
| `add_sleeve` | 66418 Позиция → Доп. втулка | — |
| `material` / `material_type` | 1138 Позиция → Вид сырья / 84633 → Тип сырья | — |
| *(скрытые)* | 1077 Обеспечение, 1076 Заказанное количество | — |

## 14. `sleeve_tasks` (queryId 184151) — задачи на втулки

Отчёт-источник «Задач на втулки» (1080): таблица подчинена ПОЗИЦИИ заказа, плоско
(`object/1080` без `F_U`) она не читается. Читают два рабочих места: пульт втулкореза
(§3.6 в `atex_workplaces.md` — свой втулкорез и свой день, серверными фильтрами
`FR_cutter_id`, `FR_task_date`/`TO_task_date`) и «Планирование производства» — кнопка
«Дэшборд» (#4774), без фильтров.

| Колонка (`t100`) | Источник (`t28`) | Функция (`t104`) | Зачем |
|---|---|---|---|
| `task_id` | 1080 Задача на втулки | 85 (abn_ID) | адрес записи отметки (`_m_set`) |
| `task_date` | 1080 Задача на втулки (главное значение) | — | плановый старт, Unix |
| `cutter` / `cutter_id` | 1180 → Втулкорез | — / 85 | фильтр пульта `FR_cutter_id` |
| `qty` / `fact` | 16399 Кол-во / 1183 Кол-во факт | — | план и факт нарезки |
| `started` / `finished` | 54315 Начато / 27163 Закончено | — | статус выводится из полей (своего поля «Статус» нет) |
| `position_id` | 1076 Заказанное количество | 85 (abn_ID) | **#4774:** чья это задача |

**#4774 (`position_id`, добавлена в `ateh` и `ateh1` 17.08.2026):** связи «задача ↔ задание»
в схеме нет (#4631), поэтому готовность втулок к заданию читается через ЕГО ПОЗИЦИИ
(«Обеспечения» задания → позиция → её задачи). По дате задачи с заданиями не сводятся:
`task_date` — это старт резки на момент СОЗДАНИЯ задачи, и после переносов плана он
расходится с текущим плановым днём задания. Колонка добавлена как обычная колонка запроса
(`_m_new/28?up=184151` с `t28=1076`, `t104=85`, `t100=position_id`); join к родительской
таблице 1:1, число строк отчёта не изменилось (`ateh` 544, `ateh1` 476). Клиент к её
отсутствию устойчив: нет колонки → группа «Втулки не готовы» не показывается, и РМ говорит
об этом вслух (молчаливое «втулки готовы» было бы враньём).

**Отчёт `packer` у планирования (#4774).** Ту же кнопку «Дэшборд» кормит `packer` (§11),
вызываемый БЕЗ `FR_packer_no`: упаковщику нужно одно упаковочное место, диспетчеру — все.
Из строк складываются `qty_fact` («Сделано») и `packed` («Упаковано») по заданию; задания
без события «Резка» отчёт не отдаёт вовсе, и это верно — по ним ещё не работали.
