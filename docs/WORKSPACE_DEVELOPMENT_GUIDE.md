# Букет промптов для разработки рабочих мест

Документ собирает **правила разработки рабочих мест** (workspaces) — HTML-шаблонов
`templates/*.html`, которые встраиваются в `templates/main.html` и отдаются
серверным шаблонизатором. Он предназначен и для разработчиков, и для AI-ассистентов:
прежде чем создавать новое рабочее место или менять существующее, свериться с
соответствующим разделом.

Правила собраны из кода и закрытых тикетов
(<https://github.com/ideav/crm/issues?q=is%3Aissue%20state%3Aclosed>). По соглашению,
принятому в `js/integram-table.js`, каждое правило снабжено ссылкой на источник:
номер задачи `(issue #NNN)` и/или путь к файлу с номером строки `file:line`. Это
позволяет проверить корректность каждого утверждения и проследить его происхождение.

> **Первоисточник.** Первая попытка собрать единый документ по разработке —
> [`integram-app-workflow.md`](integram-app-workflow.md) — теперь заархивирована рядом
> с руководством (issue #2810). Оригинал лежал в репозитории ideav/slitterplan,
> недоступном извне, поэтому документ приложен к issue и сохранён здесь. В нём, по
> замечанию из issue #2797, много ошибок, поэтому настоящее руководство собрано заново
> из проверяемых источников, а корректность каждого раздела проверяется отдельной
> задачей (см. таблицу ниже). Архивная копия приведена для сверки и отслеживания
> происхождения правил (issues #2808, #2810).

> **Как читать ссылки.** `issue #NNN` — закрытый тикет ideav/crm, в котором правило
> появилось. `file:line` — место в коде, подтверждающее правило. Номера задач вида
> `#6451`, встречающиеся в `mcp-server/`, относятся к отдельному трекеру MCP-сервера, а
> не к ideav/crm; в таких случаях указан путь к файлу как источник истины.

## Где включается это правило (подсказка)

Чтобы правила применялись при создании рабочих мест, ссылки на этот документ
размещены там, куда смотрят разработчик и AI-ассистент:

- `CLAUDE.md` — инструкция для AI-ассистента (основное место «подсказки»);
- `README.md` — раздел «Development Rules» и «Related Documentation»;
- `templates/main.html` — комментарий-указатель в шапке шаблона-контейнера, в который
  встраиваются все рабочие места.

## Проверка корректности по разделам

Каждый раздел проверяется отдельной задачей (создано по issue #2797), чтобы можно
было независимо проверить корректность информации и собрать данные для исправления.

| # | Раздел | Трекинг-задача |
|---|--------|----------------|
| 1 | Шаблонизатор и переменные `{_global_.*}` | #2799 |
| 2 | Подключение CSS/JS и версионирование ресурсов | #2800 |
| 3 | Вызовы API `_m_*` | #2801 |
| 4 | Токен XSRF и хелпер `intApi` | #2802 |
| 5 | Чтение данных из отчётов | #2803 |
| 6 | Чтение данных из таблиц | #2804 |
| 7 | Компонент IntegramTable | #2805 |
| 8 | Модальные окна и запрет `alert`/`confirm`/`prompt` | #2806 |
| 9 | Подсказки для пользователя (`js/hints.js`) | #2807 |

---

## 1. Шаблонизатор и переменные `{_global_.*}`

Рабочее место — это фрагмент HTML, который сервер вставляет в `templates/main.html` и
обрабатывает встроенным шаблонизатором (`index.php`, функции `Make_tree` и
`Parse_block`).

### 1.1. Подстановка переменных

- Синтаксис подстановки: `{_global_.ИМЯ}`. **Пробелы внутри фигурных скобок
  недопустимы** — пишите `{_global_.version}`, а не `{ _global_.version }`
  (`README.md:70`).
- Доступные глобальные переменные (объявлены в `templates/main.html:127-136`):

  | Переменная | Значение |
  |------------|----------|
  | `{_global_.z}` | имя базы данных |
  | `{_global_.action}` | идентификатор текущего рабочего места |
  | `{_global_.user}` / `{_global_.user_id}` | логин и числовой ID пользователя |
  | `{_global_.role}` / `{_global_.role_id}` | роль и её ID |
  | `{_global_.token}` | токен сессии |
  | `{_global_.xsrf}` | XSRF-токен для POST-запросов (см. раздел 4) |
  | `{_global_.id}` | ID текущего объекта/ресурса |
  | `{_global_.grants}` | base64-JSON прав доступа: `JSON.parse(atob('{_global_.grants}'))` (`templates/main.html:135`) |
  | `{_global_.version}` | версия приложения для cache-busting (см. раздел 2) |

- Кроме `_global_`, шаблонизатор понимает префиксы `{_request_.X}` (значение из
  `$_GET`/`$_POST`), `{_block_.X}` (вложенный блок) и `{_parent_.X}` (переменная
  родительского блока) — обрабатываются в `index.php` (`Parse_block`).
- Если хотя бы одна подстановка в блоке не разрешается, блок **не выводится**
  целиком. Не оставляйте «висячих» переменных.

### 1.2. Блоки и встраивание рабочего места

- Повторяемые и условные секции размечаются парами комментариев
  `<!-- Begin: ИМЯ -->` … `<!-- End: ИМЯ -->` (`templates/main.html:189,195,382,384`).
- Условный блок проверяет инлайн-комментарий вида `<!--{_global_.z}-->`: если
  переменная не разрешается, блок пропускается (`templates/main.html:189`).
- Маркер `<!-- File: a -->` (`templates/main.html:376`) динамически подключает шаблон
  рабочего места по имени `{_global_.action}.html` (например `dict.html`,
  `forms.html`). Если файла нет — подставляется `info.html`. Так рабочее место и
  встраивается в `main.html`.
- Имена блоков могут начинаться с `&`, например `<!-- Begin:&Uni_Report -->`
  (`templates/report.html:1`).

---

## 2. Подключение CSS/JS и версионирование ресурсов

- **Стили — только в `.css`, скрипты — только в `.js`.** Инлайн-стили и инлайн-скрипты
  в шаблонах не используются (`README.md:78`). Исключение — короткая логика
  инициализации (например, вызов `initHints`, см. раздел 9);
  пример применения исключения — блоки `<style>` и `<script>` для логики подсказок
  в `templates/table.html:15-260`.
- **URL ресурсов рекомендуется снабжать версией** для сброса кэша:
  `href="/css/file.css?{_global_.version}"`, `src="/js/file.js?{_global_.version}"`
  (`templates/dash.html:1,73`; `README.md:79`).
  Правило применяется в новых шаблонах (`dash.html`, `migr.html`); часть
  старых шаблонов (`table.html`, `query.html`, `info.html` и др.) версию не указывает —
  это техдолг, а не допустимое исключение.
- Порядок подключения для рабочего места с таблицей: сначала CSS компонента, затем
  его JS, затем `hints.js` (`templates/table.html:10-13`).

---

## 3. Вызовы API `_m_*`

Все команды `_m_*` — это **POST-запросы** к `/{db}/_m_…`. Они изменяют данные, поэтому
всегда требуют XSRF-токен (см. раздел 4) и обычно — параметр `?JSON` для JSON-ответа.

| Команда | Назначение |
|---------|------------|
| `_m_new/{typeId}` | создать объект типа `typeId` |
| `_m_save/{objectId}` | сохранить **первую колонку** (имя/идентификатор объекта) |
| `_m_set/{objectId}` | задать значения **остальных реквизитов** (не первой колонки) |
| `_m_del/{objectId}` | удалить объект (каскадно с подчинёнными) |
| `_m_del_select` | массовое удаление выбранных записей (`templates/object.html:545`) |
| `_m_ord/{objectId}` | установить позицию в порядке сортировки (`issue #1617`) |
| `_m_up/{objectId}` / `_m_down/{objectId}` | сдвинуть запись вверх/вниз (`templates/forms.html:2479`) |
| `_m_move/{objectId}` | сменить родителя (параметр `up={newParentId}`) |

- **`_m_save` — только для первой колонки**, для всех прочих реквизитов используйте
  `_m_set` (`issue #775`; `js/integram-table.js:5633`).
- Частые параметры строки запроса:
  - `JSON` — получить ответ в формате JSON;
  - `up={parentId}` — родитель создаваемого объекта; для корневого объекта `up=1`
    (`issue #1658`);
  - `t{reqId}={value}` — значение реквизита по его ID; можно несколько:
    `&t271=...&t273=...` (`templates/upload.html:959-961`);
  - `full=1` — **обязателен** при записи длинных/HTML-полей, иначе значение
    обрезается до 127 символов (источник истины — комментарии в
    `mcp-server/index.js:2122`).
- Пример (создание + установка реквизитов из `templates/upload.html:959-961`):

  ```js
  // создать набор: тип 269, родитель up=1, реквизиты t271, t269, t273
  intApi('POST','_m_new/269?JSON&up=1&t271=UPLOAD&t269='+encodeURIComponent(name)+'&t273='+payload,'save-set');
  // обновить реквизит t273 у существующего объекта
  intApi('POST','_m_set/'+objId+'?JSON&t273='+encodeURIComponent(payload),'save-set');
  ```

---

## 4. Токен XSRF и хелпер `intApi`

- **Каждый POST-запрос обязан содержать токен `_xsrf`.** GET-запросы токен не требуют.
- Токен берётся из шаблонной переменной: `var xsrf = '{_global_.xsrf}'`
  (`templates/main.html:128`).
- В рабочих местах используется хелпер `intApi(method, url, branch, vars, index)`,
  который **сам подставляет токен** (`templates/upload.html:820-832`):
  - если `vars` — объект `FormData`: `vars.append('_xsrf', xsrf)`;
  - если `vars` — строка: `vars = '_xsrf=' + xsrf + '&' + vars` и заголовок
    `Content-Type: application/x-www-form-urlencoded`.
- Для обычной HTML-формы добавляйте скрытое поле:
  `<input type="hidden" name="_xsrf" value="{_global_.xsrf}">`
  (`templates/edit_obj.html:77`).
- Аналогичные хелперы (`myApi`, `newApi`) есть в `templates/sql.html`,
  `templates/form.html`, `templates/quiz.html`, `templates/object.html` — они инжектят
  токен тем же способом. Не дублируйте логику XSRF вручную, если хелпер уже есть.

---

## 5. Чтение данных из отчётов

- Источник `report`: данные берутся из готового отчёта по
  `GET /{_global_.z}/report/{id}?JSON` (`templates/query.html:4`).
- Формат ответа: `{ columns: [...], data: [...], header: "..." }`, где `columns` —
  описания колонок (`id`, `type`, `format`, `name`, `granted`, `ref`), а `data` —
  колонко-ориентированный массив (компонент сам преобразует его в строки).
- Параметры запроса:
  - `LIMIT={offset},{count}` — постраничная загрузка;
  - `ORDER={colId}` / `ORDER=-{colId}` — сортировка по возрастанию/убыванию;
  - фильтры `FR_{colId}=`, `TO_{colId}=`, `F_{colId}=` (`issue #547`, `issue #549`);
  - `RECORD_COUNT=1` — получить только число записей.
- Экспорт для отчётов разрешён всегда (`issue #1469`).

---

## 6. Чтение данных из таблиц

- Источник `table`: сначала метаданные `GET /{_global_.z}/metadata/{typeId}`, затем
  данные `GET /{_global_.z}/object/{typeId}/?JSON_OBJ&F_U={parentId}&LIMIT=…`
  (`templates/table.html:4`).
- Формат `JSON_OBJ` — массив записей `[{ "i": …, "u": …, "o": …, "r": [...] }]`:
  - `i` — ID записи, `u` — ID родителя, `o` — порядок, `r` — массив значений колонок.
- Метаданные содержат список реквизитов (`reqs`) и поле `granted`: значение `WRITE`
  означает право на запись (форма редактируемая), иное — режим только для чтения
  (`issue #1508`).
- Полезные параметры:
  - `F_U={parentId}` — фильтр по родителю;
  - `F_I={recordId}` — фильтр по конкретной записи (`issue #563`);
  - `RECORD_COUNT=1` — число записей.
- Экспорт для таблицы доступен, только если в метаданных `export="1"` (`issue #1469`).

---

## 7. Компонент IntegramTable

Таблицы и отчёты в рабочих местах рендерит компонент `IntegramTable`. Подробности — в
[`TABLE_COMPONENT_README.md`](../TABLE_COMPONENT_README.md).

### 7.1. Инициализация

- **Через data-атрибуты** (авто-инициализация по `data-integram-table`):

  ```html
  <!-- отчёт -->
  <div data-integram-table
       data-api-url="/{_global_.z}/report/{_global_.id}?JSON"
       data-page-size="20"
       data-cookie-prefix="tasks-table"
       data-instance-name="tasksTable"></div>

  <!-- таблица -->
  <div data-integram-table
       data-api-url="/{_global_.z}/metadata/{_global_.id}"
       data-page-size="20"
       data-cookie-prefix="tasks-object"
       data-instance-name="tasksTable"></div>
  ```

  (`templates/query.html:1-8`, `templates/table.html:1-8`)
- **Программно**: `new IntegramTable('container-id', { apiUrl, dataSource, tableTypeId,
  parentId, recordId, pageSize, cookiePrefix, title, instanceName, onCellClick,
  onDataLoad })` (`templates/calendar.html:1328`).
- `parentId` берётся из опции или из URL (`parentId` / `F_U` / `up`); `recordId` —
  фильтр по записи (`issue #563`).

### 7.2. Правило сборки бандла

- `js/integram-table.js` — **автогенерируемый бандл, его НЕЛЬЗЯ редактировать напрямую**
  (`CLAUDE.md:1-7`).
- Исходники — модули `js/integram-table/*.js`. После правки модуля запустите
  `bash build.sh` из корня и закоммитьте оба файла: изменённый модуль и пересобранный
  `js/integram-table.js` (`build.sh`).

---

## 8. Модальные окна и запрет `alert`/`confirm`/`prompt`

- **Никогда не используйте** `alert()`, `confirm()`, `prompt()`. Вместо них —
  модальные методы (`README.md:41,67-69`):
  - `showDeleteConfirmModal(message)` — подтверждение удаления;
  - `showErrorModal(message)` — ошибки (в `MainAppController`);
  - `showWarningModal(message)` — предупреждения (в `IntegramTable`).

---

## 9. Подсказки для пользователя (`js/hints.js`)

- Всплывающие подсказки-онбординг в рабочем месте реализуются через общий механизм
  `/js/hints.js`. Сам механизм **не меняется** при добавлении нового рабочего места — в
  шаблон кладётся только HTML-структура подсказок и вызов `window.initHints({...})`.
- Полные требования к окнам подсказок — в
  [`docs/HINTS_REQUIREMENTS.md`](HINTS_REQUIREMENTS.md) (issues #1271, #1273, #1278,
  #1280; PR #1272, #1279, #1281).
- Состояние хранится в cookies: `hints_seen_workspaces` (список просмотренных рабочих
  мест), `hints_mode=off` (глобально отключает подсказки).

---

*Документ создан по issue #2797; ссылка на первоисточник
([`integram-app-workflow.md`](integram-app-workflow.md)) добавлена по issue #2808, а сам
первоисточник заархивирован в репозиторий по issue #2810. Источники правил — код
репозитория и закрытые тикеты ideav/crm; см. ссылки `issue #NNN` и `file:line` в каждом
разделе.*
