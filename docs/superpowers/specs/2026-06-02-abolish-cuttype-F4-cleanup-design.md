# Эпик «Упразднение Типа резки». Подпроект F4: зачистка (orders / cut-calc / cut-map / доки / PR)

**Дата:** 2026-06-02
**Эпик:** [[atex_epic52_planning]]. F2 (схема), F1 (ядро cut-layout), F3 (production-planning) готовы. F4 — финал: убрать хвосты «Тип резки», показать «Срок изготовления», починить cut-map, ретайр cut-calc, доки, PR ветки `epic-abolish-cuttype`.

## Объём

### 1. orders.js — «Срок изготовления» на форме заказа + зачистка cutType
- **Добавить «Срок изготовления» (DATE)** по образцу уже существующей «Дата согласования» (#3090):
  - **ЗАКАЗ:** поле в ORDER_FIELDS (`names:['Срок изготовления']`), колонка в шапке таблицы заказов, ячейка в строке, и в `buildCreateOrderRequest` (`put('dueDate', opts.dueDate)`); ввод даты в форме создания — **неприметно** (рядом с «Дата создания»/статусом, обычный `<input type=date>`).
  - **ПОЗИЦИЯ:** поле в POSITION_FIELDS (read-only, как «Дата согласования»), колонка в шапке позиций + display-ячейка в строке/черновике. НЕ добавлять в EDITABLE_POSITION_CELLS (устанавливается при планировании/наследуется).
  - Резолв реквизита по имени «Срок изготовления» (метаданные); поле опционально — нет в метаданных → тихо пропустить.
- **Зачистка остатков «Тип резки»** (фича C убрана #3089, остались defensive-ссылки): убрать `cutType` из POSITION_FIELDS и связанные мёртвые ветки (`position_cut_type*` в rowsToOrders, loadRefOptions для cutCol) — они no-op (колонка снята в F2), удалить для чистоты. Не трогать рабочую логику заказов/позиций.

### 2. cut-calc.js — РЕТАЙР (управлял дропнутым «Тип резки»; редактор полос теперь в production-planning F3)
- Удалить файлы: `download/atex/js/cut-calc.js`, `download/atex/css/cut-calc.css`, `templates/atex/cut-calc.html`, `experiments/atex-cut-calc.test.js`.
- Убрать пункт меню «Калькулятор типов резки» из `docs/atex_menu.json` (роли Admin/Диспетчер).
- На live удалить запись меню (системная таблица Menu 151) «Калькулятор типов резки» (href=cut-calc) — мной, чтобы пункт исчез у пользователей. (Проверить: ядро `calc` не используется другими — подтверждено картой.)

### 3. cut-map.js — Полоса из Резки (Тип резки упразднён)
- Убрать `TABLE.cutType`/`CUT_REQ.cutType`/`meta.cutType` и проверку метаданных «Тип резки» (сейчас инициализация падает — таблицы нет).
- `loadCut` → грузить Полосы **напрямую**: `object/{Полоса}/?JSON_OBJ&F_U={cutId}` (Полоса подчинена Резке). Удалить `loadCutType`.
- Ширина входа (джамбо) для `computeLayout` — из **Партии сырья резки → Вид сырья «Ширина, мм»** (как в F3); если не достать — fallback на сумму ширин полос (визуализация без остатка). Резолв «Полоса»/«Вид сырья»/«Партия сырья» по имени.
- Чистое ядро `layout.expandSegments/computeLayout/widthPercent` и тест `experiments/atex-cut-map.test.js` — НЕ менять (синтетические данные).

### 4. Документация
- `docs/atex_workplaces.md`: cut-calc — ретайр (раздел 2 «Калькулятор типов резки» пометить упразднённым/удалить; #2912 закрыт эпиком); cut-map (3.7) — полосы из Резки; orders (3.1) — «Срок изготовления» на форме. Таблица РМ §2 — убрать «Тип резки» из колонок где упоминается как сущность.
- `docs/atex_menu.json` — без cut-calc.

### 5. PR
- Запушить ветку `epic-abolish-cuttype` в форк `unidel2035`, открыть PR в `ideav/crm` (base main). Тело: весь эпик (F1 ядро cut-layout, F2 схема выполнена на live, F3 production-planning, F4 зачистка), что на live уже сделано (дропнут Тип резки, Полоса→Резка, отчёты cut_strips/preferable_widths/position_due_date, «Срок изготовления»), что осталось задеплоить (update.php: cut-layout.js, production-planning.js+css, orders.js, cut-map.js; удаление cut-calc.* ; live-удаление пункта меню — мной).

## Критерии приёмки
- orders.js: «Срок изготовления» показывается на форме заказа (заказ + позиция), создаётся/читается; нет мёртвых cutType-веток; `test-issue-2911-atex-orders.js` зелёный (поправлен под изменения).
- cut-calc.* удалены; пункт меню убран (json + live); ничего не ссылается на cut-calc; остальные сьюты зелёные.
- cut-map.js грузит полосы из Резки (F_U={cutId}), не падает без «Тип резки»; `atex-cut-map.test.js` зелёный; модуль грузится в node.
- Доки обновлены; PR открыт.

## Вне F4
- Точная оптимизация раскладки/комбинирование сверх жадности; UX-полировка панели полос; авто-простановка «Срок изготовления» из логики — позже.

## Деплой
update.php (Андрей): production-planning.js(+css), cut-layout.js, orders.js, cut-map.js; удаление cut-calc.js/.css/.html. Live-правки (пункт меню) — мной. Отчёты/схема — уже на live (F2/F3).
